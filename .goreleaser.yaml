version: 2

project_name: gokazi

before:
  hooks:
    - go mod download

builds:
  - binary: gokazi
    main: ./main.go
    env:
      - CGO_ENABLED=0
    flags:
      - -trimpath
      - -tags=safe
    ldflags:
      - -s -w
      - -X github.com/foomo/gokazi/cmd.version={{.Version}}
    goos:
      - windows
      - darwin
      - linux
    goarch:
      - amd64
      - arm64

archives:
  - formats: [ tar.gz ]
    format_overrides:
      - goos: windows
        formats: [ zip ]

release:
  prerelease: auto

changelog:
  use: github-native

brews:
  - repository:
      owner: foomo
      name: homebrew-tap
    caveats: "gokazi -h"
    homepage: "https://github.com/foomo/gokazi"
    description: "Simple daemonmless process manager"
    test: |
      system "#{bin}/gokazi --version"

dockers:
  - use: buildx
    goos: linux
    goarch: amd64
    dockerfile: Dockerfile
    image_templates:
      - '{{ if eq .Prerelease "" }}foomo/gokazi:latest-amd64{{ end }}'
      - 'foomo/gokazi:{{ .Version }}-amd64'
      - '{{ if eq .Prerelease "" }}foomo/gokazi:{{ .Major }}-amd64{{ end }}'
      - '{{ if eq .Prerelease "" }}foomo/gokazi:{{ .Major }}.{{ .Minor }}-amd64{{ end }}'
    build_flag_templates:
      - '--pull'
      # https://github.com/opencontainers/image-spec/blob/main/annotations.md#pre-defined-annotation-keys
      - '--label=org.opencontainers.image.title={{.ProjectName}}'
      - '--label=org.opencontainers.image.description=Project Oriented SHELL (gokazi)'
      - '--label=org.opencontainers.image.source={{.GitURL}}'
      - '--label=org.opencontainers.image.url={{.GitURL}}'
      - '--label=org.opencontainers.image.documentation={{.GitURL}}'
      - '--label=org.opencontainers.image.created={{.Date}}'
      - '--label=org.opencontainers.image.revision={{.FullCommit}}'
      - '--label=org.opencontainers.image.version={{.Version}}'
      - '--platform=linux/amd64'

  - use: buildx
    goos: linux
    goarch: arm64
    dockerfile: Dockerfile
    image_templates:
      - '{{ if eq .Prerelease "" }}foomo/gokazi:latest-arm64{{ end }}'
      - 'foomo/gokazi:{{ .Version }}-arm64'
      - '{{ if eq .Prerelease "" }}foomo/gokazi:{{ .Major }}-arm64{{ end }}'
      - '{{ if eq .Prerelease "" }}foomo/gokazi:{{ .Major }}.{{ .Minor }}-arm64{{ end }}'
    build_flag_templates:
      - '--pull'
      # https://github.com/opencontainers/image-spec/blob/main/annotations.md#pre-defined-annotation-keys
      - '--label=org.opencontainers.image.title={{.ProjectName}}'
      - '--label=org.opencontainers.image.description=Project Oriented SHELL'
      - '--label=org.opencontainers.image.source={{.GitURL}}'
      - '--label=org.opencontainers.image.url={{.GitURL}}'
      - '--label=org.opencontainers.image.documentation={{.GitURL}}'
      - '--label=org.opencontainers.image.created={{.Date}}'
      - '--label=org.opencontainers.image.revision={{.FullCommit}}'
      - '--label=org.opencontainers.image.version={{.Version}}'
      - '--platform=linux/arm64'

docker_manifests:
  # basic
  - name_template: 'foomo/gokazi:latest'
    image_templates:
      - 'foomo/gokazi:latest-amd64'
      - 'foomo/gokazi:latest-arm64'
    skip_push: auto

  - name_template: 'foomo/gokazi:{{ .Version }}'
    image_templates:
      - 'foomo/gokazi:{{ .Version }}-amd64'
      - 'foomo/gokazi:{{ .Version }}-arm64'
    skip_push: auto

  - name_template: 'foomo/gokazi:{{ .Major }}'
    image_templates:
      - 'foomo/gokazi:{{ .Major }}-amd64'
      - 'foomo/gokazi:{{ .Major }}-arm64'
    skip_push: auto

  - name_template: 'foomo/gokazi:{{ .Major }}.{{ .Minor }}'
    image_templates:
      - 'foomo/gokazi:{{ .Major }}.{{ .Minor }}-amd64'
      - 'foomo/gokazi:{{ .Major }}.{{ .Minor }}-arm64'
    skip_push: auto
